require 'rails_helper'

RSpec.describe Day, type: :model do
  describe 'associations' do
    # it { should have_many (:orders) }
    # it { should have_many (:orders).with_foreign_key('day_date') }
  end
end
